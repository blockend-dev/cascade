# web — Phase 10 (not yet implemented)

Minimal demonstration frontend. Never a trust boundary — every claim it
displays must be independently verifiable from on-chain state and public
evidence via the SDK, not trusted because the frontend says so.
