{
  "name": "@cascade/relayer",
  "version": "0.1.0",
  "private": true,
  "description": "Permissionless usage-proof submission service for Cascade — liveness/convenience only, never a source of truth.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "ethers": "^6.13.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "@types/node": "^20.14.2"
  }
}
