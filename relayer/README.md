# relayer — Phase 5 (not yet implemented)

Permissionless usage-proof submission service. Liveness/convenience only —
see `docs/trust-model.md`. Any number of independent relayer instances may
run against the same `AttributionSettlement` contract; none are privileged.
