{
  "name": "@cascade/indexer",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only event indexer for Cascade — projects the on-chain event stream into a queryable, reconstructible database. Never a protocol authority; the chain always wins. See docs/indexer.md.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/indexer/src/main.js",
    "dev": "ts-node src/main.ts"
  },
  "dependencies": {
    "ethers": "^6.13.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "@types/node": "^22.10.2"
  }
}
