{
  "name": "@cascade/wrapper",
  "version": "0.1.0",
  "private": true,
  "description": "Cascade-authored attested serving wrapper (Level 1). Verifies a model's content-addressed commitment before loading it, fails closed on mismatch. See docs/protocol-spec.md and ADR 0011.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "ethers": "^6.13.1",
    "@0gfoundation/0g-storage-ts-sdk": "^1.2.11"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "@types/node": "^20.14.2"
  }
}
